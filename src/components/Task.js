import React, { PropTypes } from 'react';

const Task = (props) => {
  return (
    <div>
      <button></button>
    </div>
  );
};

Task.propTypes = {
  task: PropTypes.object, 
};

export default Task;